# 2026-04-06 開發日誌 (V2)

## 工作摘要
完成 `index.html` 的全面重構，改為「風格對照」佈局，並補齊了 Checkbox 的標準化實作。

## 變更記錄
### 1. 展示頁面重構 (index.html)
- **架構調整**: 採用三欄式 (Grid) 對照佈局，水平並排顯示 Material 3, Cupertino, Soft UI。
- **組件覆蓋**: 透過內部 `camelot-theme` (mode="cupertino/soft") 實現同一頁面內的局部多風格並存。
- **動態初始化**: 修正腳本以支援動態選取多個 `demo-select` 與 `demo-tabs` 並填入資料。

### 2. 組件標準化 (Checkbox)
- **CamelotCheckbox**: 
    - 新增 `.color` 屬性 (primary/secondary/tertiary)。
    - 統一事件名稱為 `change` (原本為 `checked-changed`)。
    - 對齊各風格實作，確保色彩變體運作正常。

### 3. 多風格對照組件名單
- 按鈕 (Buttons)
- 選取項 (Switch, Radio, Checkbox)
- 輸入框 (Input, Select)
- 標籤 (Badge)
- 容器 (Card)
- 導航 (Tabs)

## 技術決策
- **局部封裝**: 為了測試組件對主題變數的響應能力，在 `index.html` 中大量使用局部 `camelot-theme` 嵌套，這證明了 `CamelotBaseElement` 的風格偵測邏輯是強健的。
